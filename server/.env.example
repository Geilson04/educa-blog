# Copy this file to `.env` and adjust the values to match your local setup.
# PostgreSQL connection string. Replace `user`, `password`, `localhost` and
# `port` with your database credentials. The database `teacheraid` will be
# created automatically by Prisma migrations if it doesn't exist.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/teacheraid?schema=public"

# Secret key used for signing and verifying JSON Web Tokens. You should
# replace this with a long, random string in production.
JWT_SECRET="replace-with-a-long-random-string"